interface:
  display_name: "Handoff Guard"
  short_description: "Model-aware handoffs and execution preflight"
  default_prompt: "When automatic emission is allowed, create a structured handoff; never append a Work handoff after implementation unless the user explicitly requests one. Route the next task to the cheapest sufficient model and run preflight before execution."
